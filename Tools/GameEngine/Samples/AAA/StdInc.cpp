#include "StdInc.h"

/*
int strtoken( char *str, char *separator, char *token[] )
{
	int i = 0;
	token[0] = strtok_s( str, separator );

	while( token[i] ) {
		i++;
		token[i] = strtok_s( NULL, separator );
	}

	return i;
}
*/
<?xml version="1.0" encoding="UTF-8"?>
<datasources>
    <local-tx-datasource>
        <jndi-name>jdbc/gsDB</jndi-name> 
        <connection-url>jdbc:h2:${jboss.server.data.dir}${/}h2${/}gsdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;AUTO_SERVER=TRUE;AUTO_RECONNECT=TRUE</connection-url> 
        <driver-class>org.h2.Driver</driver-class> 
        <user-name>sa</user-name>
        <password></password> 
    </local-tx-datasource>
    
    <local-tx-datasource>
        <jndi-name>jdbc/httpDB</jndi-name> 
        <connection-url>jdbc:h2:${jboss.server.data.dir}${/}h2${/}httpdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;AUTO_SERVER=TRUE;AUTO_RECONNECT=TRUE</connection-url> 
        <driver-class>org.h2.Driver</driver-class> 
        <user-name>sa</user-name>
        <password></password> 
    </local-tx-datasource> 
</datasources> 
